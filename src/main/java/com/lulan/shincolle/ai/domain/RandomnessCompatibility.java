package com.lulan.shincolle.ai.domain;

/** Compatibility level required for a decision's observable randomness. */
public enum RandomnessCompatibility {
    NONE,
    VALUE_DISTRIBUTION,
    SEQUENCE
}
